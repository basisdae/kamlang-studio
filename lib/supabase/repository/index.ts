/**
 * @deprecated Prefer lib/repositories and lib/services
 * Thin re-exports for older imports.
 */
export { getBiRepositories } from "../../repositories";
export { pingWorkspace } from "../service";
